# 小车工程接线说明

本文档按当前工程 `SysConfig` 和代码中的实际引脚整理，方便排查接线问题。

注意事项：

- 所有模块必须共地：MSPM0 开发板 GND、电机驱动 GND、传感器 GND、电池 GND 要连在一起。
- MSPM0 GPIO 是 3.3V 逻辑，传感器/模块信号脚不要直接输入 5V。
- 电机电源建议单独供电，不要直接从 MCU 3.3V/5V 给电机供电。
- 编码器模块自带上拉，编码器 A/B 信号在 SysConfig 中不要再开内部上下拉。

## 1. 串口 UART0 / VOFA 上位机

串口用于 VOFA 数据输出、串口调参等。

| 功能 | MCU 引脚 | 连接到上位机/USB-TTL |
|---|---:|---|
| UART0_TX | PA10 | USB-TTL RX |
| UART0_RX | PA11 | USB-TTL TX |
| GND | GND | USB-TTL GND |

串口参数：

| 参数 | 值 |
|---|---:|
| 波特率 | 115200 |
| 数据位 | 8 |
| 校验位 | 无 |
| 停止位 | 1 |

VOFA 当前主要使用 FireWater 格式，例如：

```text
samples:左目标,左反馈,左PWM,右目标,右反馈,右PWM
```

## 2. 电机驱动模块

当前代码中：

- 左电机使用 `AIN1/AIN2 + PWM_C0`
- 右电机使用 `BIN1/BIN2 + PWM_C1`

| 功能 | MCU 引脚 | 连接到电机驱动 |
|---|---:|---|
| 左电机 PWM | PA26 | PWMA / 左 PWM |
| 左电机方向 1 | PA24 | AIN1 |
| 左电机方向 2 | PA25 | AIN2 |
| 右电机 PWM | PA27 | PWMB / 右 PWM |
| 右电机方向 1 | PB25 | BIN1 |
| 右电机方向 2 | PB24 | BIN2 |
| GND | GND | 电机驱动 GND |

电机驱动供电建议：

| 电机驱动端子 | 接法 |
|---|---|
| VM / VIN / Motor VCC | 电机电池正极 |
| VCC / Logic VCC | 3.3V 或模块要求的逻辑电源 |
| GND | 与 MCU、电池共地 |
| STBY / EN | 若模块有该脚，需要拉高使能，接 3.3V 或按你的模块说明连接 |

如果发现左右轮方向反了，优先在代码中调整电机方向或编码器方向，不要随意交叉乱接所有线。

## 3. 左右轮编码器

编码器使用 GPIO 中断 + AB 相四倍频计数。

当前代码映射：

| 轮子 | 编码器相位 | MCU 引脚 | 说明 |
|---|---|---:|---|
| 右轮 | E1A | PA14 | GPIO 中断输入 |
| 右轮 | E1B | PA15 | GPIO 中断输入 |
| 左轮 | E2A | PA16 | GPIO 中断输入 |
| 左轮 | E2B | PA17 | GPIO 中断输入 |

编码器供电：

| 编码器端子 | 接法 |
|---|---|
| VCC | 按编码器模块要求接 3.3V 或 5V |
| GND | MCU GND |
| A 相 | 对应 E1A/E2A |
| B 相 | 对应 E1B/E2B |

重要：

- 编码器自带上拉，SysConfig 中输入上下拉保持 `NONE`。
- 如果某个轮子前进时计数方向反了，优先修改 `Inc/encoder.h` 中的方向宏：

```c
#define ENCODER_LEFT_DIR                  (-1)
#define ENCODER_RIGHT_DIR                 (1)
```

当前编码器参数：

| 参数 | 值 |
|---|---:|
| 电机编码器线数 | 13 |
| AB 四倍频 | 4 |
| 减速比 | 1:20.409 |
| 轮径 | 48mm |
| 测速周期 | 10ms |

## 4. 八路灰度传感器模块

当前使用串行通信读取八路灰度。

| 功能 | MCU 引脚 | 连接到灰度模块 |
|---|---:|---|
| DAT / DATA | PA22 | 数据输出 |
| CLK | PB20 | 时钟输入 |
| VCC | 3.3V 或模块要求电压 | 电源 |
| GND | GND | 地 |

当前通道映射：

| 物理位置 | 灰度通道 | 原始 bit |
|---|---:|---:|
| 最左 | CH1 | bit1 |
|  | CH2 | bit2 |
|  | CH3 | bit3 |
|  | CH4 | bit4 |
|  | CH5 | bit5 |
|  | CH6 | bit6 |
|  | CH7 | bit7 |
| 最右 | CH8 | bit0 |

当前工程中循迹有效电平：

```c
#define LINE_TRACK_ACTIVE_LEVEL           (0U)
```

也就是：

- 白底/无黑线：通常为 `1`
- 压到黑线：对应通道为 `0`

如果实际模块输出相反，需要修改 `Inc/line_track.h` 中的 `LINE_TRACK_ACTIVE_LEVEL`。

## 5. ICM42688 陀螺仪/IMU

ICM42688 使用硬件 SPI1，CS 使用普通 GPIO 手动控制。

| ICM42688 功能 | MCU 引脚 | 说明 |
|---|---:|---|
| SCK / SCLK | PB09 | SPI1 SCLK |
| PICO / MOSI / SDI | PB08 | MCU 输出到 IMU |
| POCI / MISO / SDO | PB07 | IMU 输出到 MCU |
| CS / NSS | PB06 | 普通 GPIO 手动片选 |
| VCC | 3.3V | 建议使用 3.3V |
| GND | GND | 共地 |

注意：

- 当前代码中 ICM42688 的 `WHO_AM_I` 正常值应为 `0x47`。
- CS 空闲为高电平，通信时拉低。
- 如果读数全 0 或 WHO_AM_I 不对，优先检查 SCK/PICO/POCI/CS 是否接反。
- 若模块丝印是 MOSI/MISO：
  - MCU `PICO` 接模块 `MOSI/SDI`
  - MCU `POCI` 接模块 `MISO/SDO`

## 6. 蜂鸣器

当前蜂鸣器为低电平触发。

| 功能 | MCU 引脚 | 连接到蜂鸣器模块 |
|---|---:|---|
| BZ 控制信号 | PB05 | 蜂鸣器 IN / SIG |
| VCC | 3.3V 或 5V | 按模块要求 |
| GND | GND | 共地 |

低电平触发含义：

| MCU 输出 | 蜂鸣器状态 |
|---|---|
| 低电平 | 响 |
| 高电平 | 关闭 |

如果上电就响，重点检查：

- PB05 是否被正确配置为输出；
- 蜂鸣器是否真的是低电平触发；
- 蜂鸣器 VCC/GND 是否接反；
- 是否用了会拉低信号脚的模块。

## 7. LED 指示灯

| 功能 | MCU 引脚 | 说明 |
|---|---:|---|
| LED 控制 | PB22 | 到点/完成提示 |

当前代码中：

- `board_notify_led_on()`：PB22 输出高电平
- `board_notify_led_off()`：PB22 输出低电平

## 8. 当前未在工程中启用的计划模块

以下是之前规划过，但当前工程代码/SysConfig 中没有正式启用的模块。接线前建议先在 SysConfig 中配置好，再写驱动。

| 模块 | 建议引脚 | 功能要求 |
|---|---|---|
| I2C OLED | PB02 / PB03 | I2C SCL/SDA，需确认具体哪一个做 SCL/SDA |
| 四个按键 | 待定 GPIO | 普通 GPIO 输入，建议上拉/下拉明确 |
| ESP8266 WiFi | 待定 UART | UART TX/RX，注意 3.3V 供电和电流能力 |
| 蓝牙模块 | 待定 UART | UART TX/RX，注意与 VOFA 串口是否冲突 |

## 9. 快速排查清单

### 小车不动

- 电机驱动 VM 是否有电；
- 电机驱动 GND 是否和 MCU 共地；
- STBY/EN 是否拉高；
- PWM 引脚 PA26/PA27 是否接到正确通道；
- AIN/BIN 方向脚是否接对。

### 只有一个轮子动

- 检查对应 PWM 线；
- 检查对应方向线；
- 检查电机线；
- 检查速度环 VOFA 输出中该侧 PWM 是否为 0。

### 编码器无计数

- 检查 PA14/PA15/PA16/PA17 是否接到编码器 A/B；
- 检查编码器 VCC/GND；
- 编码器自带上拉，不要重复乱加上下拉；
- 手动转轮观察计数是否变化。

### 循迹找不到线

- 用串口打印灰度原始值；
- 确认白底是否全 1；
- 确认黑线处对应通道是否变 0；
- 若输出相反，修改 `LINE_TRACK_ACTIVE_LEVEL`。

### IMU 无数据或全 0

- 检查 PB09/PB08/PB07/PB06 是否对应 SCK/PICO/POCI/CS；
- 检查 VCC 是否为 3.3V；
- 检查 GND 是否共地；
- 检查 WHO_AM_I 是否为 `0x47`。

### 蜂鸣器上电乱响

- 当前为低电平触发；
- PB05 空闲时应输出高电平；
- 如果模块换成高电平触发，需要修改 `board_notify.c`。
