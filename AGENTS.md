# C07A Repository Rules

## 1. 项目范围

- 本仓库是 TI MSPM0G3507 电赛小车工程。
- 当前工程使用 Code Composer Studio、TI ARM Clang、MSPM0 SDK 和 SysConfig。
- MCU、SDK、编译器、SysConfig 和构建系统的具体版本必须从当前 `.cproject`、`empty.syscfg` 和构建文件中重新读取，不得凭记忆填写。
- 具体版本事实应保存在项目理解 Skill 的 references 中，不应在长期全局规则中重复固定。
- 项目事实可能随代码变化。每个任务开始时必须重新检查相关文件，不得凭记忆沿用旧结论。

## 2. 证据等级

- A：代码、配置或构建文件直接确认。
- B：根据代码推测，需人工确认。
- C：需原理图、开发环境或实车验证。
- 不得把 B/C 写成确定事实。
- “构建通过”不得写成“实车验证通过”。
- “模块存在”不得写成“模块正在运行”。

## 3. 最小修改原则

- 优先最小范围修改。
- 修改前列出目标文件和预期修改范围。
- 不得顺带重构无关稳定模块。
- 不得覆盖或回退用户已有未提交改动。
- 发现无关问题时先报告，不自行扩大修改范围。

## 4. 公共接口规则

- 修改函数声明、结构体、宏、全局变量或公共接口前，必须搜索全部定义和调用点。
- 新增 `.c` 文件时必须检查是否加入 CCS 构建列表。
- 不得只修改头文件或只修改单个调用点。
- 删除接口前必须确认没有其他调用路径。

## 5. SysConfig 和硬件边界

- `empty.syscfg` 和生成的 `ti_msp_dl_config.*` 证明软件配置，不单独证明真实接线。
- 旧接线说明可能过时。
- 不得擅自修改引脚、外设实例、PWM 通道、波特率、I2C 地址或 GPIO 功能。
- 涉及引脚或外设配置变化时，必须先做一致性审查。
- 真实接线方向、供电、电平和硬件型号未知时标记 C。

## 6. 中断与实时性规则

- ISR 只执行必要的采样、计数、缓冲、置标志和清中断。
- 禁止在 ISR 中加入 `delay_ms()`、OLED 刷新、阻塞 UART 发送、I2C 轮询、复杂解析或控制台输出。
- 主循环中的 OLED、VOFA、蓝牙回包和 I2C 轮询均可能改变控制周期。
- 不得仅根据循环末尾 `delay_ms(10)` 声称周期严格为 10 ms。

## 7. 调度唯一性

- 每个周期任务只能有一个明确调度入口。
- 不得让同一控制更新函数同时在主循环、SysTick 和定时器中重复调用。
- 修改任务调度时必须列出初始化位置、调用位置、目标周期和时间源。
- 编码器测速、速度 PID、角度环和循迹环的调用周期必须分别明确，并保证依赖关系、数据更新顺序和离散控制参数相互匹配；不得未经分析强行统一周期。

## 8. 控制与执行器所有权

- 修改控制逻辑前必须搜索所有速度目标和电机输出写入点。
- 任何时刻必须有明确的最终电机 PWM 写入路径。
- 不得新增未经仲裁的 `motor_set_pwm()` 调用。
- 角度环、循迹环和其他上层控制不得无规则覆盖左右轮目标速度。
- 修改控制周期时必须同步检查 PID 离散实现、编码器测速周期、积分和微分项。
- 修改参数时必须说明参数物理意义、代码缩放、修改原因、预期影响和验证方法。

## 9. 安全操作

- 涉及烧录、启动电机、空载轮测、落地运行、提高 PWM 上限或提高目标速度时，必须先获得用户明确授权。
- 未经授权只允许只读分析、静态检查和用户明确允许的构建验证。
- 不得因为代码编译成功就建议直接上地高速测试。
- 实车测试应按低限幅、架空轮测、低速落地、逐步提高的顺序进行，但不得自动执行。

## 10. 验证报告

- 每次修改后必须报告修改了哪些文件。
- 必须报告哪些检查实际执行。
- 必须报告是否构建。
- 必须报告是否产生或更新构建产物。
- 必须报告哪些内容尚未验证。
- 必须报告是否仍需要实车确认。

## 11. Skill 路由

- 项目入口、当前运行模块、数据流、项目基线和证据分级问题优先使用 `c07a-project-orientation`。
- 引脚、SysConfig、UART/I2C/PWM/GPIO、外设实例和接线冲突问题优先使用 `c07a-hardware-syscfg-guard`。
- 静态检查、构建验证、发布审查和烧录前风险清单优先使用 `c07a-verify-release`。
- PID、编码器测速、角度环、循迹环、单位、符号、周期、限幅和调参问题优先使用 `c07a-control-loop-work`。
- 单一驱动模块新增、修复、接口接入、中断缓冲和模块集成优先使用 `c07a-embedded-module-work`。
- 复杂任务可以按职责组合多个 Skill。
- 不得使用项目理解 Skill 代替控制环或模块开发 Skill。
- 不得使用硬件配置 Skill 代替 PID 分析。
- 所有代码修改完成后应交由验证 Skill 检查。
- 不要在本文件复制各 Skill 的详细 references 内容；详细项目知识保存在 Skill references 中。
